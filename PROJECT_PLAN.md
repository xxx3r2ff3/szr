# 数字人系统 (SZR2026) 核心重构与功能演进规划 (2026-09-20 起)

> 声明：本规划为真实功能与工程推进台账，剥离过往自动化测试桩与过度包装的虚假门禁，以实际用户可用功能、本地模型推理与端到端链路为唯一衡量标准。边做边规划，动态追踪。

---

## 一、 系统核心目标与运行底座

- **目标定位**：全本地化独立运行的数字人播报与创作系统（零云端依赖、零外部网络出网、零外部鉴权）。
- **执行根与工作区**：
  - 权威开发源：`candidate/`（纯源码树）
  - 真实运行根：`run_root/`（内置运行时环境、权重模型、第三方引擎及静态前端）
  - 源码与运行根严格保持双向同步（candidate 为真源）。

---

## 二、 真实架构链路

```
[前端工作台 (local_ui)]
      │
      ├── 一键训练: 上传素材(mp4/wav) ──> FunASR 自动转写 ──> 本地资产归一化 (human_data/{models,voices})
      │
      ├── 短视频生成: 输入长文案 ──> cut_text 分段 ──> qftts(GPU加速/CUDA) ──> pydub 拼接 ──> heygem 离线逐帧渲染 ──> human_data/works/
      │
      └── 实时直播: SRS 本地推流服务 ──> local_engine 编排 ──> 实时口型渲染 ──> WebRTC/HTTP-FLV 播放
```

---

## 三、 已完成并经验收的功能里程碑 (截至 2026-09-20)

### 1. 一键训练（模特 / 声音）
- [x] **训练页链路实装**：上传视频/录音 → 本地资产制备 → 后台线程入列与状态机轮询。
- [x] **声音免手填参考文本**：集成 FunASR 模块，留空时自动识别并转写参考文本。
- [x] **纯名称资产管理**：移除无效预览卡，采用紧凑列表展示（一行一名 + 编辑/删除），新资源排前。
- [x] **资源命名校验与完整性约束**：中文名合法，拒绝重名与路径保留字符；过滤缺失 live.mp4 的残缺资产。

### 2. 短视频生成管线
- [x] **heygem 离线渲染管线修复**：解决平级导入路径冲突（排除屏蔽标准库的 util/）、改产物存在性判定规避 taskkill 伪失败、stderr 错误捕获。
- [x] **qftts TTS 引擎修复**：补齐参数缺失解析（args.speed 等）、修复 output_path 契约、自动创建 temp 目录。
- [x] **长文案分段合成**：通过 cut_text 切段分批调用 qfttsClone 再行拼接，突破 5 分钟超时限制。
- [x] **三段式进度与状态动画**：创建短视频页实现“①合成语音 → ②离线渲染 → ③完成”指示器、实时百分比进度条与滚动日志区。
- [x] **界面冗余功能精简**：移除未接线的“图生视频”标签与素材上传冗余按钮。

---

## 四、 当前攻坚与已闭环卡点

### 1. TTS 引擎 CUDA (GPU) 加速修复 —— 【已解决】
- **现象**：qftts 在 CPU 下运行 RTF≈10（1 秒音频耗时 10 秒），导致长文案耗时达到 5 分钟级别。
- **根因**：
  1. Windows Python 3.8+ 默认不再从系统 PATH 加载 DLL，需通过 `os.add_dll_directory` 显式注册。
  2. ONNXRuntime 1.17.1 GPU 版依赖 CUDA 11.8 / cuBLAS 11 / cuDNN 8.x。
  3. `cudnn_cnn_infer64_8.dll` 缺失关键系统依赖 `zlibwapi.dll` 导致 LoadLibraryEx 报 1114/126 错误，静默降级为 CPUExecutionProvider。
- **处置与闭环**：
  - [x] 补齐 `zlibwapi.dll` 至运行时动态库目录。
  - [x] 在 `api_v2.py` 初始化前显式注入 CUDA 11.8 及 ONNXRuntime CAPI 路径。
  - [x] 实测 `text_encoder.onnx` 与 `fm_decoder.onnx` 成功挂载 `CUDAExecutionProvider`。

### 2. 101 字长文案端到端实测验证 —— 【已解决并生成成片】
- [x] 使用 `wdd` (模特) + `wv` (声音) 输入 101 字文案执行全流程生成。
- [x] 分段语音合成在 GPU 加速下快速出片，heygem 离线渲染逐帧跑通（668 帧）。
- [x] 产物已落盘：`human_data/works/work_260920164825.mp4`（1080×1920，25.0 fps，26.72 秒，音量 max -0.4dB，16.5MB）。

### 3. 控制面自洽与双端代码同步 —— 【已验证】
- [x] `candidate/` 与 `run_root/` 全量源码及静态前端 100% 对齐。
- [x] `controlctl validate` 校验通过（结构 0 / 规则 0 / 引用 0）。

---

## 五、 后续演进阶段规划 (边做边规划)

| 阶段 | 目标 | 关键任务 | 优先级 |
|---|---|---|---|
| **Phase 1: 渲染与合成效能闭环** | 彻底解决耗时瓶颈与超时隐患 | 1. 验证 ORT CUDA 提速效果<br>2. 验证长文本分段拼接稳定性<br>3. 验证端到端出片耗时稳定在 1 分钟左右 | P0 (当前) |
| **Phase 2: 交互与异常体验加固** | 提升本地客户端交互体验 | 1. 生成中防重复点击与状态锁定<br>2. 历史作品管理（预览/下载/删除）<br>3. 离线渲染失败时的友好错误拦截 | P1 |
| **Phase 3: 实时直播与离线管线统一** | 本地直播与短视频复用底层渲染能力 | 1. local_engine 与 works_service 资源复用<br>2. 避免直播与离线渲染并发争抢显存<br>3. 优化 GPU 显存自动释放机制 | P2 |
| **Phase 4: 一体化本地打包与分发** | 便捷启动与全环境自洽 | 1. 启动脚本环境前置自检（显卡驱动/CUDA/DLL）<br>2. 统一依赖核验与增量发布包 | P3 |
